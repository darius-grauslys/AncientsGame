#include <test_util.h>
#include <defines.h>

MunitResult assert_item_stack_allocation_specifier_as__empty(
        Item_Stack_Allocation_Specifier *p_item_stack_allocation_specifier);

DECLARE_SUITE(item_stack_allocation_specifier)
