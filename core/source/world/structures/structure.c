#include <world/structures/structure.h>

void initialize_structure(
        Structure *p_structure) {
    debug_abort("structure.c, not impl");
}

void add_p_room_to__structure(
        Structure *p_structure,
        Room *p_room) {
    debug_abort("structure.c, not impl");
}
